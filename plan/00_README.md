# 프로젝트 계획서 — TRsim (Tracking Radar Simulator)

**최종 갱신**: 2026-05-02 (v0.40 — Physics Lab 추가, plan 19 신설, 차별점 5+1, 11 Plugin Protocol)

**작성일**: 2026-04-22
**대상 프로젝트**: TRsim — 오픈소스 추적 레이더 워크벤치 플랫폼
**상태**: 설계 단계 종료, 구현 진입 가능 (v0.35 — 결정 43개, 블로커 0)

---

## 이 문서는 무엇인가

추적 레이더 알고리즘·자원·시각화를 DLC 패키지로 자유롭게 확장하는 **오픈소스 워크벤치
플랫폼 TRsim** 의 기획·아키텍처 계획서. v0.16~v0.35 설계 + v0.36 정합성 검토 + v0.37 sim_3d 분리 + v0.38 HIL 통합 + v0.39 Reference Timing + v0.40 Physics Lab.

## 한 줄 요약

> **TRsim은 추적 레이더 알고리즘·자원·시각화·물리 모델을 자유롭게 다루는 오픈소스
> 워크벤치 플랫폼이다. Apache 2.0 코어에 커뮤니티가 만든 `.trsim-pkg` 를 더해, 어떤
> 추적 시나리오라도 시뮬·검증·NN 학습이 가능하며, HIL 통합으로 실제 펌웨어·하드웨어를
> 시뮬 루프에 연결한 검증까지 지원한다. Physics Lab 으로 물리 모델을 3D 시각화·검증·진화시킬 수 있다.**

## 핵심 방향 (합의 완료, v0.40 기준)

| 항목 | 결정 |
|---|---|
| 정체성 | 추적 레이더 IDE + DLC 확장 + HIL 통합 + Physics Lab 가능 오픈소스 플랫폼 |
| 라이선스 | Apache 2.0 (코어), DLC 자유 (v0.35) |
| 플랫폼 | Python 3.11+ · 크로스 플랫폼 (Win/Linux/Mac, Q-P1) |
| UI 프레임 | PySide6 (LGPL) + QMainWindow + QDockWidget + 커맨드 팔레트 |
| 시각화 스택 | pyqtgraph (2D plots) + PyVista (3D Scene, v0.28) |
| **세 Workspace** (v0.40) | **Editor (자원 작성) ↔ Simulator (시뮬 실행) ↔ Physics Lab (물리 검증·진화)** |
| Plugin 시스템 | MVP: 단일 .py 동적 로드. MVP+α: `.trsim-pkg` DLC 시스템 (v0.35, 17) |
| SDK Layer | `trsim.sdk` 안정 Public API — **11 Plugin Protocol** (v0.40: PhysicsModelProtocol 추가) |
| 베이스라인 (v0.34) | Two-ray multipath + Multi-scatterer/Glint + EKF/UKF + GNN + Refraction + OS-CFAR |
| 물리 검증 | 분석 공식 + Stone Soup·MATLAB 비교 + 17~20+ 종 검증 시나리오 (v0.40: Physics Lab 안 통합) |
| 거버넌스 | BDFL → Core Team (v0.35, GOVERNANCE.md) |
| **HIL 통합** (v0.38) | MVP+α Phase 8 — DUTAdapter Protocol + TCP/JSON, GT/SIL/HIL 3-way 비교. 상세: [18](plan/18_hil_integration.md) |
| **Reference Timing** (v0.39) | PerformanceClock + Frame Profiler + Lock-step Handshake — Phase 2/3/4 분산. 상세: [18 § 18.16~18.17](plan/18_hil_integration.md) |
| **Physics Lab** ⭐ (v0.40) | **MVP+α Phase 9 — 3-pane 인터랙티브 + 9 Test Objects + 4 시간 모드 + 사용자 물리 plugin. 상세: [19](plan/19_physics_lab.md)** |
| 구현 방침 | v0.16~v0.35 계획서 기반 신규 작성 (옛 코드 회귀 비교 X) |
| 마감 | 없음 (논리적 의존성 순서로 진행) |

## 문서 구성 — 이 순서로 읽는 걸 권장

사용자 요청 순위에 따라 번호 순서대로 읽으면 된다.

| # | 파일 | 내용 | 분량 |
|---|---|---|---|
| 01 | `01_vision_scope.md` | 프로젝트 정체성, MVP 범위, 사용자 시나리오, In/Out of scope | 중 |
| 02 | `02_architecture.md` | 계층 설계, 블록도, 스레드 모델, 의존 규칙 | 상 |
| 03 | `03_data_model.md` | 시나리오 스키마, DSP Contract, Probe 포맷, Run 스키마 | 상 |
| 04 | `04_migration.md` | 신규 구현 순서, Phase 0~9, 완료 기준 | 중 |
| 05 | `05_ui_ux.md` | DockWidget 배치, 패널 목록, 툴바, 커맨드 팔레트 | 중 |
| 06 | `06_topics.md` | Probe/Trace 시스템, 테스팅 전략, 기타 기술 주제 | 상 |
| 07 | `07_nn_integration.md` | NN Plugin, StageSlot, Dataset, Training | 상 |
| 08 | `08_radar_waveforms.md` | Waveform 1급화, Pipeline Graph, Waveform-conditional Slot | 상 |
| 09 | `09_radar_platforms.md` | 레이더 플랫폼 (Maritime/Fixed Ground), Installation, DEM 차폐 | 중 |
| 10 | `10_workspaces.md` | Editor / Simulator Workspace 분리 구조 (v0.40: + Physics Lab) | 중 |
| 11 | `11_coordinate_systems.md` | 좌표계 + Vertical Reference + DEM Import + Coherence Validator | 중 |
| 12 | `12_placement_and_motion.md` | 정적 배치 vs 동적 운동, MotionKind 5종, 건물 anchor, wave 응답 | 중 |
| 13 | `13_editor_workspace.md` | Editor Workspace 상세 — Activity + 탭 + 사이드바, 5개 Activity 패널 | 중 |
| 14 | `14_dynamics_model.md` | 사실적 표적 동역학 — 6DOF rigid body, AIRCRAFT/POWERED_FLIGHT/BALLISTIC | 중 |
| 15 | `15_atmosphere_model.md` | 대기 모델 — ISA 표준 + rain attenuation + 시각 fog + 4/3 refraction | 중 |
| 16 | `16_baseline_audit.md` | 베이스라인 점검 — MATLAB·Stone Soup·RadarSimPy 비교, 5종 MVP 추가 + Physics Lab 통합 | 상 |
| 17 | `17_open_platform.md` | 오픈소스·DLC 에코시스템·거버넌스 — Apache 2.0 + .trsim-pkg + Core team + 11 Plugin Protocol | 상 |
| 18 | `18_hil_integration.md` | HIL 통합 — DUTAdapter + GT/SIL/HIL 3-way 비교 (v0.38) + Reference Timing (v0.39) | 상 |
| **19** | **`19_physics_lab.md`** | **🆕 Physics Lab — 3-pane 인터랙티브 + 9 Test Objects + 4 시간 모드 + 사용자 물리 plugin (v0.40)** | **상** |
| A | `appendix_A_code_audit.md` | 🚫 **DEPRECATED** — sim_3d 평가 (정상 동작 미검증, 무효) | 낮음 |
| B | `appendix_B_glossary.md` | 용어집 (Contract, Plugin, Run, Probe, HIL, Reference Timing, Physics Lab 용어 등) | 소 |

## 문서 상태 표기 규칙

각 섹션 말미에 다음 태그를 붙인다.

- ✅ **Decided** — 합의 완료, 대화 기록에 근거 있음
- 🟡 **Proposed** — 기본값/추천으로 문서에 썼지만 너가 다시 봐야 함
- ⏳ **TBD** — 의도적으로 미결정. MVP 진입 전 합의 필요
- 🔒 **Out of scope** — MVP 범위 밖, 미래 확장용 메모만

## 다음 단계

1. 이 계획서를 네가 **순서대로 읽으며 검토**
2. 🟡 Proposed 항목 → 동의/반대 피드백
3. ⏳ TBD 항목 → 대화로 결정
4. 모든 ✅ 확정되면 → 새 레포 생성 + 뼈대 코드 작업 시작

## 변경 이력

| 버전 | 날짜 | 변경 |
|---|---|---|
| v0.1 | 2026-04-22 | 초안 작성 |
| v0.2 | 2026-04-22 | 07 NN 통합 섹션 추가 (StageSlot, Dataset, Training, TF↔numpy 이식) |
| v0.3 | 2026-04-22 | 07 § 7.1 원칙 3 재작성 (학습 데이터도 Pipeline 경유) + § 7.4.6 단계적 학습 전략 신규 |
| v0.4 | 2026-04-22 | 07 원칙 6 신규 (NN 부분 교체 opt-in) + 케이스 0 (단일 스테이지 교체, Pairing 기준 예시) + § 7.6.4 Run Manifest + 03 RunResult 확장 |
| v0.5 | 2026-04-22 | **08 신규** — 실제 DSP 펌웨어 코드 분석 결과 반영. Waveform 1급화, Pairing의 본질 재정의(CW↔FMCW 매칭), Pipeline Graph(DAG), Target Gate Slot, FMCW_TRIANGLE이 MVP 유일 지원 Waveform |
| v0.6 | 2026-04-22 | **08 재작성**. v0.5의 과도한 추론 철회: 본 프로젝트는 FMCW Triangle 단독이며 CW+FMCW Hybrid는 별개 레이더 모델. RadarModel 추상화 도입(FMCW Triangle 구현 1개 + 미래 자리). Pairing Contract 단일화, 07/01/용어집 동반 정리. `target_pairing.c`가 미구현 상태라 Pairing 개발 자체가 Workbench의 주요 가치 사례가 됨 |
| v0.7 | 2026-04-22 | 08 § 8.3.5 신규 — Target Gate를 "Tracker 보조 옵션"으로 재정의. `TrackerContract.requires_target_gate` 속성 도입, 3조건 AND 활성화 규칙, NN Tracker와의 관계, 멀티 타겟 지원이 1급 요구사항임을 명시 |
| v0.8 | 2026-04-22 | **프로젝트 정체성 재정의** — "Tracking Radar 추적 성능 검증 워크벤치"로 명확화. Selected Target 1급 개념 도입, Closed-Loop Tracking 전제 명시. 01/03 전면 개편: 정체성·MVP·Journey·Scenario 필드·Metrics를 전부 선택 표적 추적 중심으로 재편. Metrics가 Primary/Secondary/Overall/System 4계층으로 분리되고 Positioner Lag 같은 추적 특화 지표 추가 |
| v0.9 | 2026-04-22 | **일관성 통과** — 05/07의 UI 목업과 메트릭 예시를 v0.8의 추적 지표 체계로 교체. 05 Run Panel 메인/상세 목업, 07 Result Panel·Run 목록·Compare UI의 Pd/Pfa 표기가 Primary Target 추적 지표(Continuity, IDSw, Positioner Lag)로 갱신 |
| v0.10 | 2026-04-22 | **Plugin 워크플로 구체화 + Scope POV 추가**. 05 § 5.3.2 3D Scene View를 3rd-person + Scope POV 이원 구조로 재작성(빔 폭 원, EKF 명령점, 십자선). 05 § 5.3.3 Plugin Manager에 Workspace 디렉토리 구조 및 "New Plugin 마법사" 4단계 명시. 07 `DatasetBuildJob`에 `training_target: TrainingTargetRef` 필드 추가 — Dataset↔Trainer↔Plugin 연결 고리 명시화. Properties 패널에 EKF Command vs Positioner Actual 구분 표시. 용어집에 Workspace/Scope View/New Plugin 마법사/Training Target 등 5개 추가. **UI 목업 v0.2**로 위 내용 모두 반영 |
| v0.11 | 2026-04-23 | **시뮬레이션 물리 요소 전수 검토**. 7개 영역 × 50여 항목을 🔴/🟡/🟢로 분류. MVP에 4개 물리 항목 추가: 함선 자세(roll/pitch), RCS aspect 의존, 안테나 사이드로브, 자함 동요. 03 data model에 `RCSModel` Protocol·`SimpleAspectRCSModel`·`PlatformDynamics`·사이드로브 파라미터 추가. 06에 **"Deferred Physics" 섹션 신설** — 6개 Suite로 그루핑(Weather / Sea Clutter / Advanced RF / Interference / Target Signature / RF Hardware). 용어집에 물리 관련 6개 용어 추가 |
| v0.12 | 2026-04-23 | **NN 학습 데이터 추출 구체화**. 07에 § 7.4.5a **Dataset Variant** 신설 — 물리 조건을 점진적으로 쌓은 Variant_A~D로 bias/variance 진단 가능. § 7.4.5b에 **Pairing NN 구체 SampleSpec 템플릿** (pairing.toml 예시, 가변 길이 up/down 매칭 행렬 라벨, Primary 정보 불포함, 시뮬 기반 GT 생성 로직 설명). 용어집에 Dataset Variant 추가. Pairing NN이 Wave 1 최우선으로 확정됨 |
| v0.13 | 2026-04-23 | **Workbench 운용 모드 분리** — DSP 모드(기본)와 NN 개발 모드 명시적 분리. NN 모드는 `View > Mode` 메뉴로 전환. NN 모드가 Step 1(Dataset Extraction) · Step 2(NN Evaluation) 두 단계로 구성. Step 2에 **4-error 진단** 도입(Bayes/Training/Dev/Test + gap 자동 해석). 01 정체성 재정의, 05에 모드 분리 원칙, 07 헤더 재구성 + § 7.6 Step 2 섹션 신설, 03에 `NNEvalJob`/`NNEvalResult`/`BayesEstimator`/`RunMode` 추가. Bayes error는 고급 옵션, 기본은 3-error. MVP는 NN 모드 구조 포함, 실제 구현 NN은 Pairing 하나 |
| v0.14 | 2026-04-23 | **Run 생애주기 재정의 + 3대 통합**. ① **Program Session ≠ Run**: 환경·레이더는 Session 내내 동작, Run은 표적 trajectory 재생 구간. Run State Machine (IDLE/RUNNING/PAUSED/ENDED). Run/Pause/Stop 버튼과 Stop 저장 확인 다이얼로그. ② **Single Command Path**: `PositionerCommand` 타입 + `CommandSource` enum으로 포지셔너 명령 출처 강제. TRACKER 소스는 `source_track_id`/`source_frame_id` 필수. Run 후 Lineage 검증. ③ **방향키 수동 조작**: AUTO/MANUAL 토글, `M` 키·방향키, 초기 위치 잡기 용도. ④ **Stage I/O Panel**: 각 스테이지 입출력 실시간 UI + CSV/HDF5 다운로드. ⑤ **GT Isolation 강화**: Level 2 구조 + Level 3-1 정적 스캔 + Level 3-2 Lineage 검증 + Level 3-3 Contamination Heuristic. 03/01/05/06/용어집 전반 개편 |
| v0.15 | 2026-04-23 | **두 레이어 시간 제어** — 바깥 Simulation Clock(Start/Pause/Stop + Speed ×1/2/4/8) + 안쪽 Target Run(Run/Pause/Stop)을 독립 레이어로 분리. 03에 `SimulationClock`·`SpeedMultiplier`·`SimulationState` 신설, v0.14의 `SessionClock`을 `SimulationClock`으로 승격·개편. Sim PAUSED 중 UI 입력 버퍼링(재개 시 일괄 반영), Sim Clock이 모든 레이더·환경 물리의 기반 시간임을 명시. 05 툴바를 두 레이어로 분리 재작성, 단축키 확장 (Space=Target, Shift+Space=Sim, 1/2/3/4=Speed). Run Panel에 Time Layers 블록 신설 (Sim/Target 두 상태 병기). Target Run 버튼은 "Target 접두사"로 명시 구분. 01 불변 원칙·MVP 표·용어집 갱신. Target Run Stop 원인에 `SIM_STOPPED` 추가 |
| v0.16 | 2026-04-23 | **통합 리뷰 & 미결 질문 통합**. T0/T2/T7 묶음 처리. 02 architecture 블록도를 v0.14~v0.15 반영 재작성(`PlaybackClock`→`SimulationClock` 승격, **CommandBus**·**InputBuffer** 신설). § 2.2a CommandBus·§ 2.2b SimulationClock 섹션 신설. § 2.4 스레드 모델의 Sim 루프 재작성 (Sim/Target 두 레이어 상호작용, 버퍼 소비, Wall clock 동기화). 04 Phase 2~4에 v0.14~v0.15 모듈 체크리스트 추가. 03 § 3.7 Event Bus 카탈로그를 `sim.*` / `target.*` 네임스페이스로 재정리. **OPEN_QUESTIONS.md 신설** — 06 § 6.7·07 § 7.9의 TBD와 기타 미결 질문 통합, 우선순위·블로킹·결정 시점 명시. 블로커 0개 확인 |
| v0.17 | 2026-04-23 | **시각화 패널 인터랙션 표준 — 휠 줌·팬 + 3D 뷰 조작 모델**. 05 § 5.5.4 신설·확장. 패널별 조작 모델을 의도적으로 분리: **3D 3rd-person은 Unreal 에디터 + Maya/Blender 병용** (우클릭 비행, WASD, Alt+드래그 orbit/pan/dolly), **Scope POV·FFT·Timeline은 2D 맵 방식** (Google Maps 스타일 커서 중심 줌), **Stage I/O는 스크롤+박스 크기 줌**. **F 키 (Focus)에 레이더 추적 특성 결합** — Primary Lock 중이면 자동 추적, Lock 놓치면 직전 추적 표적의 마지막 위치 유지 ("LOST" 뱃지), fallback은 자함. Shift+F=선택 표적, Alt+F=자함. 상태 변수 `last_focused_target` 도입. Ctrl+휠 5배속, 더블클릭/R 기본 복귀, 각 패널 우하단에 줌 레벨 UI(`1.0× [-+][R]`). pyqtgraph 기본 지원 최대 활용, Trackpad pinch 매핑. Help 오버레이 (Shift+/). |
| v0.18 | 2026-04-23 | **Radar Platforms & Installation** — 레이더 플랫폼 다양화. **`plan/09_radar_platforms.md` 신규 문서** 추가. MVP 2 카테고리: Maritime(함선) + Fixed Ground(건물·타워·산정상 추상화). 플랫폼마다 `PlatformMotionModel` 추상화 (MVP: `sea_state` + `stationary`). Scenario에 `RadarPlatform` 필드 추가, `scenario_meta.toml`의 `[platform]` 섹션으로 저장. **Installation 필수 게이트** — Scenario Load 후 Target Run 전에 필수 통과 (preset 있으면 자동 적용). **Installation은 Scenario의 일부로 자동 저장** — 같은 `scenario_meta.toml`에 `[platform]` 섹션, Scenario 이름 = Installation 이름. Built-in 수정 시도 시 "새 이름으로 저장" 다이얼로그. Save As 메뉴로 Installation 실험 지원. 05 § 5.3.8 Installation 화면 신설 (Preset 선택, DEM 클릭 배치, 차폐 Preview, Coverage Stats, Save 분기). DEM 기반 **간단 LOS 차폐 검사** (직선 세그먼트 최고점 비교, n_samples=64). 좌표계 계층 정리. 01 불변 원칙·MVP 표, 03 data_model(§ 3.2.1a/b), 용어집(Radar Platform, Ownship, Platform Motion Model, Installation, LOS Obstruction) 전반 갱신. "자함(ownship)" 용어는 Maritime Platform의 관습적 별칭으로 유지. |
| v0.19 | 2026-04-24 | **두 Workspace 구조 — Editor / Simulator 분리**. **`plan/10_workspaces.md` 신규 문서** 추가. 단일 프로그램이지만 UI를 두 Workspace로 분리: **Editor Workspace**(자원 편집·조립)와 **Simulator Workspace**(실행·평가, 기존). Workspace는 DockLayout·Toolbar·CommandSet 각각 독립 구성, 상태 보존 전환. **Mode(DSP/NN)는 Simulator Workspace 안의 하위 개념**으로 재배치 — Workspace와 Mode는 다른 층위. Activity Selector UI (좌상단 탭, `Ctrl+Shift+E`/`S`/`Tab` 단축키). 전환 시 Sim RUNNING·미저장 변경 확인 다이얼로그. "Open in Simulator" / "Back to Editor" 컨텍스트 전달. DockLayout은 Workspace별 독립 파일 저장(`~/.workbench/layouts/editor.json`, `simulator.json`). Workspace 간 직접 참조 금지(EventBus/WorkspaceManager 경유). Command 네임스페이스: `editor.*`, `sim.*`/`target.*`, `workspace.*`. **MVP는 Editor Workspace 뼈대만** (ResourceBrowser + Scenario Composer), 풀 편집(Map/Radar/Targets 각 에디터)은 MVP 후. Installation(v0.18) 화면은 Editor의 Scenario Composer 안으로 통합됨. 01 한 줄 정의·MVP 표·불변 원칙, 00 README 문서 목록 갱신. |
| v0.20 | 2026-04-24 | **자원 참조 구조 + 재현성 + 패키징** — Editor Workspace 저장 구조 결정. **10 § 10.9 자원 저장 구조 (참조 기반)** 신설: Map·Radar·Targets를 독립 디렉토리(`resources/maps/`, `resources/radars/`, `resources/targets/`)로 저장, Scenario는 ID + content hash로 참조. Scenario.toml에 `[refs]` 섹션(map_id, map_hash 등), `[composition]`(고유 설정), `[platform_install]`(v0.18 Installation). **§ 10.10 재현성 — Content Hash + Run Manifest**: 각 자원에 SHA-256 content hash 자동 계산, Run Manifest의 `resource_refs`에 실행 시점 hash 기록. 자원 수정 시 Scenario에 "업데이트 있음" 뱃지, 과거 Run 재실행 시 hash 불일치 경고 다이얼로그. Resource History(MVP+α)는 옛 버전 hash 키로 보관. **§ 10.11 Reproducibility Bundle**: Scenario/Run을 단일 `.scnbundle`/`.runbundle` (tar.gz)로 export → 다른 PC에서 Import로 재현 (자원·Plugin·Manifest 전부 포함). Import 시 충돌 처리 다이얼로그(이름 변경·덮어쓰기·무시), Workbench 버전 호환성 검사. 03 § 3.5.1b.1 `RunManifest`·`ResourceRefs` 스키마 신설. 01 불변 원칙·MVP 표 갱신 (자원 참조·재현성·Bundle 3항목). |
| v0.21 | 2026-04-25 | **좌표계 정합성 + 정적/동적 위치 분리** — 이전 프로젝트의 "건물 뜸·해안선 불일치" 문제 근본 해결. **`plan/11_coordinate_systems.md` 신규**: 워크스페이스 = 단일 Map = 단일 Origin (불변). WGS84 위경도 + Map ENU 미터 좌표. **명시적 Vertical Reference** (egm96/ellipsoid_wgs84/msl_local/unknown). AWS DEM은 EGM96 기본값. DEM 샘플링은 **bilinear** 표준, edge_handling=clamp. DEM Import 파이프라인: 포맷 감지 → 메타 누락 시 사용자 다이얼로그 (EGM96 추천) → 수평/수직 변환 → 캐시. **Coherence Validator** 5종 (DEM-vertical 일관성, 해안선-해수면, 건물-DEM 정합, 해상영역, vertical_reference 명시). 검증 시점 5개 (DEM 로드/건물 배치/Targets 수정/Map 저장/Simulator 전환). **`plan/12_placement_and_motion.md` 신규**: 모든 자원의 위치를 `base_*` (정적, Editor) 와 `current_*` (동적, Sim RUNNING) 로 분리. **MotionKind 5 카테고리**: FIXED_GROUND, GROUND_VEHICLE(MVP 후), SURFACE_VESSEL, FLOATING_STATIC, AIRBORNE. v0.18 PlatformCategory와 매핑. **Wave 응답 환경/응답 분리**: Map의 SeaStateEnvironment ≠ Entity의 WaveResponseModel. 해상 응답 프리셋 (large_ship/small_boat/buoy/none). **건물 Anchor 시스템**: BASE_TO_TERRAIN(기본, DEM 자동 정합)/EXPLICIT_ALT/FLOOR_AT_MSL/TERRAIN_OFFSET 4종 + mesh_origin 보정 (BASE_CENTER 기본). 해수면 시각화: Sim RUNNING 중 sinusoidal 파도 (MVP 포함, 시각적 보조). 표적 trajectory의 z 처리 명시 (해상은 무시, 항공은 절대값). **MVP에서 표적 궤적 편집 GUI 제외** — CSV import + 메타 편집 + 시각화만. 자원 종속성 정리 (Building은 Map 종속, Targets는 약한 종속). 01 불변 원칙(2개) + MVP 표(6개) 갱신. |
| v0.22 | 2026-04-25 | **자체 규격 지형 (Workbench Native) + 편집 도구 (경량)** — 외부 DEM의 한계 회피, 사용자 편집 가능. **11 § 11.5 DEM Import 파이프라인 재작성**: 외부 DEM은 import 소스, 시뮬은 자체 규격 `terrain.npz` 사용. **§ 11.5.5 Land/Sea 구분 4 방식 사용자 선택** (자동 z<threshold / Nodata / 외부 해안선 / 모두 육상). **§ 11.10 Workbench Native Map Format 신설**: 디렉토리 구조 (map.toml, terrain.npz, coastline.npz, buildings.toml, source/), terrain.npz 스키마 (grid_east_m, grid_north_m, elevation_m, **land_mask**, resolution_m, source_dem_hash). 핵심: `land_mask=False` 영역은 sea_surface.z_at_sea_m 반환 — DEM의 부정확한 해저 z 차단. **§ 12.8.6 건물 anchor에 land_mask 검사** 추가 — 해상에 건물 배치 시도 거부. **§ 12.11 지형 편집 도구 신설**: MVP 경량 (Land/Sea Mask Brush + Spot Edit), MVP+α (Smooth, Coastline Polygon Edit, Crop, 합성). 편집 이력 추적 (Undo/Redo 50개), 원본 DEM 재import 시 편집 보존 옵션. 원본 DEM은 `source/` 폴더에 보관 — 재변환 가능 (Q3 결정). 01 불변 원칙(1개) + MVP 표(2개) 갱신. |
| v0.23 | 2026-04-25 | **03 data_model 정합 — v0.21~v0.22 타입 정식 반영**. Scenario 스키마 위에 v0.20+ 권위 노트 (`[refs]` 분리). **§ 3.2.1c Map & Workbench Native Terrain** 신설: `VerticalRefType`, `VerticalReference`, `GeoOrigin` (불변), `SeaSurface`, `WorkbenchTerrain` (grid + land_mask), `Map` 통합 정의. `sample_terrain()` 함수 — `land_mask=False`면 sea_surface.z_at_sea_m 반환. **§ 3.2.1d Placement & Motion**: `MotionKind` 5 카테고리 enum, `PlacedEntity` (base_*), `CurrentPose` (런타임), `pose_for_display()` Sim 상태별 분기. **§ 3.2.1e Wave Response**: `SeaStateEnvironment` (Map) vs `WaveResponseModel` (Entity) 분리, MVP 4 프리셋 (large_ship/small_boat/buoy/none) 코드. **§ 3.2.1f Building Anchor**: `AnchorMode` 4종 + `MeshOrigin` 3종, `BuildingEntity`, `resolve_base_altitude()` (v0.22 land_mask 검사 포함 — 해상 배치 시 InvalidPlacement 예외). **§ 3.2.1g Target Trajectory**: `TargetWaypoint`, `TargetEntity` (PlacedEntity 상속, motion_kind별 altitude_m 해석). 섹션 상태 갱신 (3.2.1a~g 7개 + 3.5.1b.1 RunManifest). |
| v0.24 | 2026-04-25 | **09 radar_platforms와 motion_kind 정합** — v0.21에서 도입된 `MotionKind`를 v0.18 `PlatformCategory`와 명시 매핑. **§ 9.3 RadarPlatform에 `motion_kind` 필드 추가** (FIXED_GROUND / SURFACE_VESSEL / FLOATING_STATIC / ...). **§ 9.4.3 PlatformCategory ↔ MotionKind 매핑 표 신설**: MARITIME → SURFACE_VESSEL(운항) 또는 FLOATING_STATIC(정박) 두 모드. FIXED_GROUND → FIXED_GROUND 자동. GROUND_VEHICLE/AIRBORNE는 MVP 후. MARITIME 정박/운항 구분 명시 — MVP는 거의 항상 정박, 운항 중 레이더는 MVP+α (별도 platform_trajectory.csv 필요). **레이더와 표적이 같은 MotionKind 추상 위에 위치** — 같은 wave_response 프리셋 공유 가능. Scenario `[platform]` TOML 예시에 wave_response 추가. 9 섹션 상태 갱신. |
| v0.25 | 2026-04-25 | **Antenna Model 일반화 + Radar Editor** — 안테나 형태(파라볼릭·평면 어레이)와 채널 구조(단일·모노펄스 4ch)를 일반화. **08 § 8.5a Antenna Model 신설**: `AntennaType` enum (parabolic / planar_array), `AntennaConfig` Protocol, `ParabolicAntenna` (sinc² 빔, 기존 v0.18 동작 유지), `PlanarArrayAntenna` (N×M 격자, array factor × element pattern, MVP는 uniform weighting). **RX 4채널 모노펄스**: `RXChannelKind` enum (Σ/Δaz/Δel/Δ²), `MonopulseRXConfig` (slope kaz/kel, channel_setup="quad_feed"/"subarray_partition"), `RXArrayConfig` 확장. 안테나 타입 ↔ RX 모드 호환 매트릭스. **모노펄스 각도 추정 Pipeline** 추가 — Σ에서 detection, Δaz/Δel에서 error_az/el_rad 계산해 Tracker 입력 (v0.13 4-error 진단의 EKF Command Error 근원). **03 § 3.2.1h Antenna Configuration** 신설 (타입 정식 정의). **05 § 5.3.9 Radar Editor 신설**: 통합 에디터 — 안테나 타입 드롭다운 선택 시 동적 폼, Beam Pattern Preview 실시간, RX 채널 모드 전환 (Single SUM ↔ Monopulse 4ch). 4종 Preset (parabolic_dish_60cm / parabolic_monopulse_60cm / planar_16x16_xband / planar_32x32_xband). 검증·경고 (spacing>λ/2 grating lobe 등). MVP 단순화 명시 — 빔 패턴은 sinc²/uniform array factor 단순 모델, 정밀 빔 패턴·MIMO·DBF는 MVP+α. Radar 자원 TOML에 [antenna], [rx_array] 섹션 추가. 01 MVP 표(2개) 갱신. |
| v0.26 | 2026-04-26 | **Editor Workspace 전체 레이아웃 — `plan/13_editor_workspace.md` 신규**. v0.19에서 미정으로 남겼던 Editor Workspace 상세 설계 완료. **§ 13.2 핵심 결정**: Activity Selector(좌, 5개) + 중앙 탭 영역 + Resource Browser 상시 사이드바 (VS Code 스타일). 5개 Activity = 🎬 Scenario Composer / 🗺 Map Editor / 📡 Radar Editor / 🎯 Targets Editor / 📂 Resource Browser. **§ 13.3 Scenario Composer (메인)**: References 블록(자원 ref + hash 상태 인디케이터), Installation 통합(v0.18을 모달이 아닌 인라인으로), Composition 메타(seed/duration/frame_rate), 실시간 Validation. **§ 13.4 Map Editor 경량**: Pan/Zoom + Land/Sea Brush + Spot Edit + Add Building + DEM Import Wizard 통합 + Edit History (Undo 50). **§ 13.5 Radar Editor**: 05 § 5.3.9 명세 그대로 통합. **§ 13.6 Targets Editor**: 메타 편집 + Trajectory Preview만, 편집은 CSV 직접. **§ 13.7 Resource Browser 전체화면**: 검색·필터·정렬·Bulk 작업. **§ 13.8 공통 동작**: 미저장 변경 다이얼로그, Coherence Validator 자동 실행, 단축키(Ctrl+1~5 Activity 전환). **§ 13.9 Workspace 전환**: Editor↔Simulator 핸드오프, 마지막 위치 복원, "Back to Editor" 컨텍스트. **§ 13.10 MVP Phase 분류**: Phase A 핵심(Activity 골격+Composer+Radar Editor+전환) → Phase B 필수(Map경량+DEM Wizard+Targets+Validator) → Phase C MVP+α. 10 § 10.3.2에 13 참조 링크. |
| v0.27 | 2026-04-26 | **사실적 표적 동역학 — `plan/14_dynamics_model.md` 신규**. 추적 레이더 검증의 신뢰도 위해 표적이 실제처럼 거동해야. v0.21의 단순 trajectory 보간 한계 극복. **§ 14.2 핵심 결정**: 자유도 타입별(AIRCRAFT/POWERED_FLIGHT 6DOF target, BALLISTIC 3DOF point-mass), MVP 레벨링(Level 1=3DOF + 외력 모델, Level 2=6DOF MVP+α), trajectory=reference·실제=동역학 적분 원칙. **§ 14.3 6DOF 표현**: `RigidBodyState` 신설 (위치/속도/자세/각속도). MVP는 자세를 velocity vector 기반 추정 (coordinated flight 가정). **§ 14.4 외력 모델**: gravity (standard g), drag (ISA atmosphere 근사), lift (trim + reference 추적 PD), thrust (constant/curve/stage), control (PD 컨트롤러). **§ 14.5 motion_kind별 사양**: **AIRCRAFT** (waypoint reference, max climb rate, bank angle, load factor), **POWERED_FLIGHT** (thrust profile + 옵션 reference, 큰 g), **BALLISTIC** (trajectory 무시, 초기 조건만). **§ 14.6 적분**: RK4, 메인 dt=0.05s + sub-step 0.005s. **§ 14.7 Trajectory Reference**: motion_kind별 z 컬럼 의미 최종 확정 — AIRCRAFT는 목표 고도 (동역학이 부드럽게 추적), BALLISTIC은 무시. **§ 14.8 표적 Preset 9종**: aircraft_fighter_jet/airliner, missile_cruise/ballistic, drone_quadcopter, artillery_shell + 기존 함정/건물. **MotionKind 5→7 확장** (AIRBORNE 폐기, AIRCRAFT/POWERED_FLIGHT/BALLISTIC 신설). 12 § 12.3 enum 갱신, § 12.4.2 compute_current_pose 동역학 분기, § 12.7.1 z 컬럼 표 갱신. 09 § 9.4.3 매핑 표 갱신. 03 § 3.2.1d MotionKind enum, § 3.2.1g 의미 재정의 (reference), § 3.2.1i Dynamics State 신설(RigidBodyState/TrajectoryReference/ThrustProfile/AircraftDynamics/PoweredFlightDynamics/BallisticDynamics). 결정 사용자 답: **A=(c) 타입별 자유도 / B=(a) 레벨링 / C=(a) 진행**. 또한 Q-P1 closed (크로스 플랫폼 Win/Linux/Mac), Q-EW1~5 OPEN_QUESTIONS 정식 등록. |
| v0.28 | 2026-04-27 | **시각화 스택 결정 + 대기 모델 신규**. **Q-P2 closed** (시각화 라이브러리: pyqtgraph 메인 + PyVista 보조 하이브리드). 02 § 2.6a 신설 — pyqtgraph는 데이터 패널(FFT/Scope/시계열/2D Map), PyVista(QtInteractor)는 3D Scene View 한정. EventBus 경유 통신, Domain·App 계층은 라이브러리 모름. **`plan/15_atmosphere_model.md` 신규**: Q-A1 결정 — 대기 상태 세 측면 모두(시각·동역학·전파). `AtmosphereState` 단일 추상 (visibility_km/sky_condition/sea_level_pressure/temperature/rain_rate_mmh/refractivity/ducting/wind). **§ 15.3 ISA 표준 대기 (Level 1 MVP, troposphere)** — air_density·temperature 고도 함수, 14 § 14.4 항력·양력에 통합. **§ 15.4 PyVista 시각** — sky color (clear/overcast/fog/rain/storm), fog distance ∝ visibility_km. **§ 15.5 Rain attenuation (ITU-R P.838 단순화)** — X-band 영역 정밀, 08 § 8.x RadarModel.compute_received_power에 통합 → 폭우 시 SNR 감소로 추적 알고리즘 검증 시나리오. MVP+α: Stratosphere/Ducting/Wind/Time-variant. Q-AT1~3 OPEN_QUESTIONS 등록. 통계: 결정 22→24, 곧 필요 6→5 (VTK 빠짐). |
| v0.29 | 2026-04-27 | **Simulation Domain — Map과 시뮬 영역 분리**. 사용자 통찰: "Map이 10×10 km인데 레이더 빔이 50 km 갈 수 있다 — 어떻게 처리?". **11 § 11.11 신설** (240줄). 두 영역 분리: **Map (DEM 정밀)** vs **SimulationDomain (시뮬 가능 전체)**. **OutsideEnvironment** (open_sea/open_land/blocked/infinite_plane) — Map 밖 처리 정책. `sample_terrain_safe()` 통합 함수 — Map 안이면 정밀, 밖이면 outside 적용. **LOS 차폐**: Map 밖은 자유 전파 가정 (지형 정보 없음). 소속: Map의 기본값 + Scenario에서 override 가능 (같은 Map으로 다른 시나리오). **Coherence Validator 6번째 검사**: Map ⊂ Domain, 레이더 max range vs Domain, trajectory in Domain, outside 일관성. Editor UI: Map Editor에 Domain Settings 패널 + Coverage Preview, Scenario Composer에 override 옵션. PyVista 시각화: Map 영역 정밀 + Map 밖 단순 평면. 03 Map 클래스에 simulation_domain·outside_environment 필드 추가, `SimulationDomain`/`OutsideEnvironment`/`sample_terrain_safe` 정의. 결정: **Q-MS1=(a) 옵션 D, Q-MS2=(d) 사용자 선택 기본 open_sea, Q-MS3=(c) Map+Scenario override 둘 다**. 결정 24→27. |
| v0.30 | 2026-04-27 | **07 NN 모드 보강 — v0.25~v0.29 정합** (T11). 모노펄스(v0.25)·동역학(v0.27)·대기(v0.28)·도메인(v0.29) 변경이 NN 학습 데이터·평가에 미치는 영향 명시. **§ 7.4.5a 확장 Variant 차원 6개**: antenna_type / rx_channels / dynamics_realism / target_motion_kind / atmosphere / outside_environment. 모든 조합(2^N) 비현실 → **선택적 Variant 6종** (A_ideal/B_realistic/C_rain/D_planar/E_far_target/F_ballistic) — 단일 axis 변형. **Sample 형식 확장**: 모노펄스 4채널 IQ + error_az/el_rad (추적 표준), 동역학 GT(velocity·attitude derived), atmosphere meta (선택), outside_environment 인식. **Variant Manifest 갱신** (각 Variant의 v0.25~v0.29 조건 명시). **§ 7.6.0 4-error 진단 영향**: 모노펄스 → 각도 추정 표준 일치 / 동역학 → data mismatch 진단 신뢰도 ↑ / atmosphere → 환경 도메인 시프트 명시 / Domain → 거리·시야 도메인 시프트 측정. **§ 7.8 영향 표 5행 추가** (08·12·14·15·11). NN 학습 데이터의 사실성·다양성 모두 강화 — Wave 1 Pairing NN 시 v0.25~v0.29 모든 조건 활용 가능. |
| v0.31 | 2026-04-27 | **04 migration Phase 체크리스트 갱신 — v0.18~v0.30 모듈 정합 (T10)**. Phase 2 중복 제거, v0.18 Platform / v0.20 ResourceLibrary·Bundle / v0.21 base/current·MotionKind·anchor·wave / v0.22 WorkbenchTerrain·DEM Import / v0.25 Antenna·Monopulse / v0.27 Dynamics 6 모듈 / v0.28 Atmosphere·rain attenuation / v0.29 SimulationDomain 모두 Phase 2에 추가. Phase 3에 WorkspaceManager·ResourceLibrary·BundleService·DEM Import 추가. Phase 4에 Editor Workspace 5 Activity (Composer/Map/Radar/Targets/Browser) + Domain Settings 패널 + Atmosphere 패널 + PyVista 3D Scene (DEM smooth shading + 파도 셰이더 + atmosphere fog) 추가. Phase 5 검증 카테고리 3→11 (안테나 빔/모노펄스 error/항공 동역학/탄도 자유낙하/ISA/rain attenuation/sample_terrain_safe/Coherence Validator 6종). **Phase 6 (NN, MVP+α) 신설** — StageSlot, NN Plugin, DataExporter, DatasetBuilder (Variant 6종), TrainerService, Evaluator (4-error). Phase 순서 원칙에 NN·v0.30 추가. 04 § 4.3 Phase 순서 ✅ 표시. |
| v0.32 | 2026-04-27 | **02 architecture 의존 그래프 전면 갱신 (T9)** — 마지막 큰 정합 작업. **§ 2.2 블록도** v0.16 → v0.31: UI Layer에 WorkspaceManager + Editor Workspace 5 Activity (Composer/Map/Radar/Targets/Browser) + Domain Settings + Atmosphere Panel + PyVista 3D Scene + NN Mode 추가. App Layer에 Resource & Workspace 그룹(WorkspaceManager/ResourceLibrary/ResourceCache/BundleService/CoherenceValidator/DEMImportPipeline) + NN 그룹(DataExporter/DatasetBuilder/TrainerService/NNEvaluator) 추가. Domain Layer에 Map & Coordinates / Placement & Motion / Dynamics / Atmosphere / Antenna / Platform 6 그룹 명시. **§ 2.3 디렉토리**: domain/dynamics/ 서브패키지 (state/params/forces/solver/motion_models/impact 6 모듈), atmosphere·rain_attenuation·antenna·rx_channels·monopulse·simulation_domain 등 신규, ui/editor/* 와 ui/simulator/scene_3d/ 와 ui/simulator/nn_mode/ 분리, app/nn/ 그룹, io/dem_import·bundle_io·workbench_native 추가. tests/physics에 11종 카테고리 명시. data/resources 신설. **§ 2.5 의존 규칙 보강**: Workspace 분리 강제(Editor↔Simulator 직접 참조 금지), Visualization 격리(pyqtgraph/pyvista는 UI에만), Resource & Validation 책임 분리(Domain은 형태만, App는 관리·검증), NN Plugin 격리(plugin_scanner AST 검사). **§ 2.9 모듈 도입 이력 신설** — v0.18~v0.31 도입된 모든 모듈을 App/Domain/UI/IO 4영역으로 표 정리. 각 모듈의 도입 버전·통찰·트리거·핵심 책임 한눈에 파악. 핵심 통찰의 흐름(v0.13~v0.31) 14단계 요약. 02 § 2.9 ✅ 표시. 섹션 상태 5개 갱신. **이로써 모든 정합 작업 완료, 구현 단계 진입 가능.** |
| v0.33 | 2026-04-27 | **Map Editor 평탄화 도구 추가**. 사용자 통찰: "맵 편집에 평탄화 기능도 있어?" — MVP 도구 4종에 빠져 있던 영역 평탄화 보강. **12 § 12.11.1에 Flatten Area 추가** (Land/Sea Brush·Spot Edit과 함께 MVP 핵심 3종). 사각형 영역 드래그 → 단일 z 값 통일. 용도: **함정 정박지·활주로·건물 부지** 평탄화 (anchor BASE_TO_TERRAIN 정합성 ↑). 옵션: Land only / Preserve buildings. 검증: 해상 영역 포함 시 경고, BASE_TO_TERRAIN building 자동 anchor 재계산, Map 50% 이상 평탄화 시 확인 다이얼로그. edit_history 기록 (op="flatten_area"). **MVP+α 확장**: 폴리곤 영역 / Brush 반경 / min/avg/max/주변 평균 모드 / feather edges. **다른 평탄화 변형**(Smooth/Cut-Fill/Slope)은 MVP+α 유지. 13 § 13.4 Map Editor UI 갱신 (Tools 패널 + 도구 표 + Edit History 예시). 04 Phase 4 map_editor에 Flatten 추가. 02 디렉토리 tools.py 주석 갱신. 결정: **Q1=(a) A 영역 평탄화만, Q2=(b) 사각형 드래그만, Q3=(a) 구체 값 입력 메인**. |
| v0.34 | 2026-04-28 | **베이스라인 점검 + 5개 MVP 추가 (Q-BL1~5 closed)**. 사용자 통찰: "이 프로그램이 생명력을 얻으려면 베이스라인 점검을 해야겠어". MATLAB Phased Array Toolbox + Stone Soup + RadarSimPy + GPARM 정밀 비교 → 차별점 (단일 추적 + DSP↔NN IDE + 4-error)은 견고하지만 **5개 베이스라인이 빠지면 비교 demo에서 신뢰도 무너질 위험** 식별. **`plan/16_baseline_audit.md` 신규** — 5종 MVP 추가 + 10종 MVP+α 명시 + 5종 의도적 제외 명시. (1) **Two-ray multipath** (Q-BL1, 08 § 8.5b.1) — 해상 sea bounce, lobing pattern, Toggle 가능. (2) **Multi-scatterer 표적 + Glint 자동 발생** (Q-BL2, 14 § 14.10) ⭐ — 점 표적은 glint 없어 우리 차별점("단일 표적 추적 안정성")의 핵심 변수 사라짐. Scatterer/ExtendedTarget 데이터 모델, 표적 Preset 9종에 scatterer 분포 (3~5개). σ_glint ≈ L_target/(2√N) 이론치. monopulse extended target 합성. (3) **EKF + UKF 선택** (Q-BL3, Stone Soup 호환) — Editor 드롭다운, 기본 EKF. (4) **GNN 다중 표적 데이터 연관** (Q-BL4) — Hungarian assignment, 다중 환경 단일 표적 시뮬 필수. (5) **Atmospheric refraction 4/3 earth** (Q-BL5, 15 § 15.5.4) — 장거리 정확도. **추가 보강**: OS-CFAR (CA-CFAR와 선택, 클러터 환경). **계획서 영향**: 03 § 3.2.1j/k 신설 (Tracker/Data Associator/Multipath/Refraction), 04 Phase 2 모듈 9개 추가 + Phase 5 검증 11→17개, 02 § 2.3 디렉토리 (propagation/scattering/tracker_ukf/data_associator) + § 2.9 모듈 도입 이력 10개 추가, 14 § 14.10 (Multi-scatterer + Glint), 15 § 15.5.4 (Refraction), 08 § 8.5b/8.5c (Multipath/CFAR), 01 MVP 표 6개 + Out of Scope 5종 (STAP/MIMO/SAR/RFS/RIS) 명시. Q-BL6~10 등록 (Frequency-dependent RCS / UKF sigma point UI / GNN gating / Multipath 정밀도 / Ducting). 결정 27→33. **MATLAB·Stone Soup 비교 demo의 신뢰 기반 확보 + 차별점 자원 집중 둘 다 가능.** |
| v0.35 | 2026-04-28 | **오픈소스 + DLC 플랫폼 정체성 전환**. 사용자 통찰: "오픈소스로 공개 + DLC 같은 확장 가능 플랫폼". `plan/17_open_platform.md` 신규 — 9개 결정 (Q1-rev/Q2~Q9). **Apache 2.0** (DLC 자유도 + 특허 grant + 기업 친화), **적극 공공** (GitHub public + Issue·PR + DCO), **핵심 계층만 개방** (알고리즘 + 자원 + 시각화 패널), **`.trsim-pkg` 패키지** (VS Code Extension 모델, manifest.toml + plugins/ + resources/ + ui/), **DLC 에코시스템** (Blender 역할 — 플랫폼 핵 + 커뮤니티 콘텐츠), **Core team 3~5명** (초기는 BDFL 임시), **awesome-list 가벼운 시작**, **Core에 SDK 포함** (`trsim.sdk` 모듈), **DLC 보안은 MVP+α**. 정체성 진화: "검증 워크벤치" → "오픈소스 워크벤치 플랫폼". **아키텍처**: Plugin SDK Layer 신설 (`src/workbench/sdk/`, Domain 위·App 아래), 의존 규칙 (sdk/↛app/ui/, domain/↛sdk/, DLC↛app/ui/domain 직접), PackageManager + ResourceLibrary 확장 (User > Packages > Built-in) + PanelRegistry, 9 Plugin Protocol (Detector/Pairing/AngleEstimator/Tracker/Predictor/Classifier/DataAssociator/Resource/UIPanel). **MVP 범위**: 오픈소스 인프라만 (LICENSE/README/CONTRIBUTING/CoC/GOVERNANCE/SECURITY/.github 템플릿/CI). DLC 시스템 자체는 MVP+α (Phase 7). **루트 문서 11개 작성** (Apache 2.0 + DCO + Contributor Covenant 2.1 외부 참조 + GitHub Actions CI). **04 Phase 0 오픈소스 인프라 11종 + Phase 7 DLC 시스템**. 01 § 1.1 가치 제안 v0.35 재작성 + MVP 표 4개 추가. 02 § 2.6b SDK Layer + § 2.5 의존 규칙 + § 2.9 모듈 도입 이력 + 통찰 흐름 18번째. Q1~Q9 closed, Q-OP1~5 등록. 결정 33→43. **"플랫폼이 핵, 커뮤니티가 콘텐츠"** — 학술·방산·교육 모든 영역 채택 가능 구조. |
